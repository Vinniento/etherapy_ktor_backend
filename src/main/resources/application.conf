ktor {
    deployment {
        port = 8000
        watch = [ classes ]
        host = "127.0.0.1"
    }
    development = true
    application {
        modules = [ fh_campus.ApplicationKt.module ]
    }
}